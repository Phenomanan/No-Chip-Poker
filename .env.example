# Comma-separated list of allowed origins for HTTP + Socket.IO
# Use * for local testing only.
CORS_ORIGINS=http://localhost:3001,https://your-username.github.io

# Optional API protection
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=200
